# @onestop/config

Shared tooling config: ESLint (`@onestop/config/eslint`) and Prettier (`@onestop/config/prettier`). The base TypeScript config is `tsconfig.base.json` at the repo root; every workspace extends it.
